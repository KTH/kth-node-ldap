{
  "name": "kth-node-ldap",
  "version": "2.3.7",
  "description": "Adds a Promise-based API on top of ldapjs as well as a filter builder.",
  "main": "index.js",
  "scripts": {
    "test": "tape \"tests/**/*-tests.js\" | faucet",
    "codecheck": "standard",
    "preversion": "npm run codecheck && npm run test",
    "postversion": "git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:KTH/kth-node-ldap.git"
  },
  "keywords": [
    "ldap",
    "ldapjs",
    "promise",
    "filter"
  ],
  "license": "MIT",
  "dependencies": {
    "async": "^2.0.1",
    "ldapjs": "^1.0.0"
  },
  "peerDependencies": {
    "kth-node-log": "^3.0.1"
  },
  "devDependencies": {
    "faucet": "0.0.1",
    "kth-node-log": "^3.0.1",
    "standard": "^10.0.2",
    "tape": "^4.6.0"
  }
}
